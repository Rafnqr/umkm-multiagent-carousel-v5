langchain==0.3.7
langchain-community==0.3.5
langchain-groq==0.2.1
chromadb==0.5.5
fastembed==0.3.6
streamlit==1.38.0
requests==2.32.3
tavily-python==0.5.0
Pillow==10.4.0
numpy==1.26.4

# Hanya untuk deploy di Streamlit Community Cloud (Linux).
# Marker "sys_platform == 'linux'" membuat pip otomatis SKIP baris ini
# di Windows/Mac lokal, jadi tidak perlu hapus manual tiap ganti environment.
pysqlite3-binary==0.5.3; sys_platform == "linux"
